# Draft — ComfyUI Discord (Comfy Org)

**Destination:** `discord.com/invite/comfyorg` — the official Comfy Org server (confirmed via web
search as the current official invite; member count claims from search results are unverified and
platform-reported, not something I confirmed directly).
**Account:** Mehran's Discord account.
**Channel:** unconfirmed — I have no account and cannot read the server's channel list or `#rules`
from outside. Likely a `#showcase`- or `#comfyui-tools`-style channel based on how servers like this
are usually structured; **Mehran needs to check the actual channel list and any posting rules live
before sending this.**
**Format:** two sentences + one image, per Discord norms — not a wall of text.

---

## Message

```
Open-source Photoshop plugin that talks to a local ComfyUI server — text to image, inpaint,
outpaint, sketch to image, live painting, and now Unflatten: splits a flat layer back into real
Photoshop layers with alpha. Free, MIT, 100% local: https://github.com/MehranMarxian/OpenLayer
```

**Attach:** `docs/assets/v0200/unflatten.webp`

---

### Notes for Mehran

- If the server has a dedicated self-promo/showcase channel with its own format (some Discords want
  a specific template — project name, tech stack, link), adapt this to match rather than pasting it
  raw.
- Don't post this in a general chat channel without checking first — that's the fastest way to get a
  message deleted or draw a mod warning in a 60k+-member server.
- This is genuinely low-stakes compared to the Reddit/HN posts — worth doing, but not worth much
  more drafting time than this.
