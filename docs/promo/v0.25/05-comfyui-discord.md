# Draft — ComfyUI Discord (Comfy Org)

**Destination:** `discord.com/invite/comfyorg` — the official Comfy Org server invite (per prior
cycle's web search; unverified fresh this round).
**Account:** Mehran's Discord account.
**Channel:** unconfirmed — no account, can't read the channel list or `#rules` from outside. Likely a
`#showcase`-style channel. **Mehran must check the actual channel list and posting norms live before
sending this.**
**Format:** two sentences + one image, per Discord norms.

---

## Message

```
Open-source Photoshop plugin that talks to a local ComfyUI server — text to image, inpaint, outpaint,
sketch to image, live painting, and now Remove Background: no prompt, no checkpoint, ~2s cutout with
real alpha, and the pixels it hands back measure identical to your source. Free, MIT, 100% local:
https://github.com/MehranMarxian/OpenLayer
```

**Attach:** the before/after Remove Background screenshot (does not exist yet — see `00-plan.md`).
Fall back to `docs/assets/v0200/unflatten.webp` only if a Remove Background image genuinely can't be
ready in time, and if so, drop the Unflatten mention from the message text above rather than let the
image and the words point at two different features.

---

### Notes for Mehran

- If the server has a dedicated self-promo/showcase channel with its own required format (project
  name, tech stack, link), adapt to match rather than pasting this raw.
- Don't post in a general chat channel without checking first.
- Low-stakes relative to the Reddit/HN posts — worth doing, not worth over-drafting.
