# Draft — ComfyUI Discord (Comfy Org)

**Destination:** `discord.com/invite/comfyorg` — the official Comfy Org server invite (per prior
cycles' web search; unverified fresh this round).
**Account:** Mehran's Discord account.
**Channel:** unconfirmed — no account, can't read the channel list or `#rules` from outside. Likely a
`#showcase`-style channel. **Mehran must check the actual channel list and posting norms live before
sending this.**
**Format:** two sentences + one image or short clip, per Discord norms.

---

## Message

```
Open-source Photoshop plugin that talks to a local ComfyUI server — text to image, inpaint, outpaint,
sketch to image, live painting, and now Layer Maps: depth, line-art and normal passes off any layer,
exact pixel size, ~3s combined, no prompt/checkpoint. Free, MIT, 100% local:
https://github.com/MehranMarxian/OpenLayer
```

**Attach:** the Layer Maps video if it exists by then; otherwise the best single still from
`layer-maps-demo-photo.md` (the terrace/depth-range shot is the strongest single frame).

---

### Notes for Mehran

- If the server has a dedicated self-promo/showcase channel with a required format (project name,
  tech stack, link), match that instead of pasting this raw.
- Don't post in general chat without checking first.
- Lowest-stakes item in the batch — worth doing if you're already sending r/comfyui, not worth
  over-drafting on its own.
