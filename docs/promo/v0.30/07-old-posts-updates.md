# Old posts / housekeeping — checked live, 2026-09-10

Re-checked discussion #104 directly via `gh api graphql` rather than assuming last cycle's plan
happened. Its body still opens with "It's been a while since the last one of these — v0.15.0 was the
last version that got its own post..." and has **no forward-pointer edit** added since — meaning the
v0.25 cycle's plan to edit it never happened, consistent with `00-plan.md`'s finding that nothing
except LinkedIn actually went out last cycle.

## What this cycle's discussion post needs to do differently

Rather than edit #104 in isolation, the new v0.30 Discussion post (`01-github-discussion.md`) should
itself state plainly that v0.25 never got a writeup — see the note already in that draft. If Mehran
wants to also add a one-line forward pointer to #104 pointing at the new post once it exists, that's a
two-minute edit, but it's not required the way it was when #104 was the *only* recent post standing in
as the "wrong" newest thing. Given #104 is now three releases behind (v0.20 → v0.25 → v0.30), the
gap is wide enough that a reader landing there would already suspect it's stale without needing the
edit to tell them so — worth doing for politeness, not load-bearing.

## Nothing else to check this cycle

The v0.25-cycle file (`docs/promo/v0.25/07-old-posts-updates.md`) recorded edits to #83, #85, #88, #94
as already applied from the *v0.20* cycle — those are still in place (I did not re-verify each one
individually this round; no reason to expect they reverted, and re-checking four discussion bodies for
a housekeeping item that was already confirmed once is lower priority than the new venue drafts).
